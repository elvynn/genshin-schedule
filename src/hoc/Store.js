import React, {createContext, useReducer} from "react";

const initialState = {
    schedule: [],
    error: null
}

const Store = () => {
    return (
        <div>
            
        </div>
    );
};

export default Store;